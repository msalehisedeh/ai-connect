/*
Login or create an account in OpenAI dashboard: https://platform.openai.com
Go to: https://platform.openai.com/settings/keys
Click the "Create new secret key" button and give the key a name (optional)
replace your-key-here with the key

For YOUR_PINECONE_API_KEY: Login or create an account in pinecone.io
Go to Console → Projects, Click Create Project, Give it a name and description, Click Create.
Each project has its own environment and API key.
Go to Console → API Keys (usually in the sidebar).
You will see a default key or can click Create Key.
For us-east1-gcp: check Projects → Your Project → Environment.
For your-index-name: Go to Indexes → Create Index, Give it a name (e.g., your-index-name), Choose dimension (matches your embeddings, e.g., 1536 for OpenAI text-embedding-3-large), Click Create.
*/
OPENAI_API_KEY=your-key-here
PINECONE_API_KEY=YOUR_PINECONE_API_KEY
PINECONE_ENVIRONMENT=us-east1-gcp
PINECONE_INDEX=your-index-name